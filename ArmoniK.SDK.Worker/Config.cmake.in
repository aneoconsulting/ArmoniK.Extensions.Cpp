@PACKAGE_INIT@

set_and_check(ARMONIK_SDK_WORKER_PREFIX "@PACKAGE_CMAKE_INSTALL_PREFIX@")

include(CMakeFindDependencyMacro)
find_dependency(ArmoniK.SDK.Common CONFIG REQUIRED)
find_dependency(ArmoniK.Api.Worker CONFIG REQUIRED)

include("${ARMONIK_SDK_WORKER_PREFIX}/lib/cmake/ArmoniK.SDK.Worker/ArmoniK.SDK.WorkerTargets.cmake")

check_required_components(ArmoniK.SDK.Worker)

include_directories("${ARMONIK_SDK_WORKER_PREFIX}/include/armonik/sdk/worker")